# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj
version: 2

before:
  hooks:
    - go mod tidy
    - go generate ./...
    - go install github.com/tc-hib/go-winres@latest
    - go-winres make --in cmd/nanoclaw-launcher/winres/winres.json --out cmd/nanoclaw-launcher/rsrc --product-version={{ .Version }} --file-version={{ .Version }}

builds:
  - id: nanoclaw
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
      - -X github.com/probnotas/nanoclaw/cmd/nanoclaw/internal.version={{ .Version }}
      - -X github.com/probnotas/nanoclaw/cmd/nanoclaw/internal.gitCommit={{ .ShortCommit }}
      - -X github.com/probnotas/nanoclaw/cmd/nanoclaw/internal.buildTime={{ .Date }}
      - -X github.com/probnotas/nanoclaw/cmd/nanoclaw/internal.goVersion={{ .Env.GOVERSION }}
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/nanoclaw
    ignore:
      - goos: windows
        goarch: arm

  - id: nanoclaw-launcher
    binary: nanoclaw-launcher
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/nanoclaw-launcher
    ignore:
      - goos: windows
        goarch: arm

  - id: nanoclaw-launcher-tui
    binary: nanoclaw-launcher-tui
    env:
      - CGO_ENABLED=0
    tags:
      - stdjson
    ldflags:
      - -s -w
    goos:
      - linux
      - windows
      - darwin
      - freebsd
    goarch:
      - amd64
      - arm64
      - riscv64
      - loong64
      - arm
    goarm:
      - "6"
      - "7"
    main: ./cmd/nanoclaw-launcher-tui
    ignore:
      - goos: windows
        goarch: arm

dockers_v2:
  - id: nanoclaw
    dockerfile: docker/Dockerfile.goreleaser
    extra_files:
      - docker/entrypoint.sh
    ids:
      - nanoclaw
    images:
      - "ghcr.io/{{ .Env.GITHUB_REPOSITORY_OWNER }}/nanoclaw"
      - "docker.io/{{ .Env.DOCKERHUB_IMAGE_NAME }}"
    tags:
      - "{{ .Tag }}"
      - "latest"
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/riscv64

archives:
  - formats: [tar.gz]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]

nfpms:
  - id: nanoclaw
    builds:
      - nanoclaw
      - nanoclaw-launcher
      - nanoclaw-launcher-tui
    package_name: nanoclaw
    file_name_template: >-
      {{ .PackageName }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "arm64" }}aarch64
      {{- else if eq .Arch "arm" }}armv{{ .Arm }}
      {{- else }}{{ .Arch }}{{ end }}
    vendor: nanoclaw
    homepage: https://github.com/{{ .Env.GITHUB_REPOSITORY_OWNER }}/nanoclaw
    maintainer: nanoclaw contributors
    description: nanoclaw - a tool for managing and running tasks
    license: MIT
    formats:
      - rpm
      - deb
    bindir: /usr/bin

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

# upx:
#    - enabled: true
#      compress: best
#      lzma: true

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
